{ 
  "scripts": {
  "dev": "mintlify dev",
  "build": "echo 'No build step required'",
  "start": "mintlify dev --port 3000",
  "vercel-build": "echo skipping static export"

  },
  "devDependencies": {
    "mintlify": "^4.2.573"
  }
}