---
title: Google OAuth
api: GET /auth/google
authMethod: none
playground: none
---

Initiates Google OAuth flow. Redirects the browser to Google's consent screen. Requires `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` to be configured.

<Note>
This is a browser redirect endpoint — not intended for direct API calls from the playground.
</Note>
